name: Build and Deploy Application

on:
  push:
    branches: [ runner_test ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
        - production
        - staging
        - stage1

jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: bike-shop/package-lock.json
        
    - name: Install dependencies
      run: |
        cd bike-shop
        npm ci
        
    - name: Run linting
      run: |
        cd bike-shop
        npm run lint || echo "Linting skipped - no script found"
        
    - name: Build application
      run: |
        cd bike-shop
        npm run build
        
    - name: Check build artifacts
      run: |
        cd bike-shop
        ls -la dist/
        
    - name: Build Docker image
      run: |
        docker build -t bike-shop-test ./bike-shop
        
    - name: Save Docker image
      run: |
        VERSION=$(date +%Y%m%d-%H%M%S)
        mkdir -p ./docker-images
        docker save bike-shop-test:latest -o ./docker-images/bike-shop-test-${VERSION}.tar
        echo "Docker image saved to: ./docker-images/bike-shop-test-${VERSION}.tar"
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: docker-image
        path: ./docker-images/*.tar
        retention-days: 1

  deploy-stage1:
    runs-on: [self-hosted, linux, x64]
    needs: build
    if: success() && (github.event.inputs.environment == 'stage1' || github.event.inputs.environment == 'production')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: docker-image
        
    - name: Transfer image to stage1 server
      run: |
        IMAGE_FILE=$(ls -t *.tar | head -1)
        scp $IMAGE_FILE stage1@192.168.1.59:~/aplikacja_biker/
        scp -r ./bike-shop/nginx.conf stage1@192.168.1.59:~/aplikacja_biker/
        
    - name: Deploy to stage1 server
      run: |
        ssh stage1@192.168.1.59 "
          # Create directory if not exists
          mkdir -p ~/aplikacja_biker
          
          # Load Docker image
          docker load -i ~/aplikacja_biker/$(basename $(ls -t ~/aplikacja_biker/*.tar | head -1))
          
          # Stop and remove old container
          docker stop bike-shop-container 2>/dev/null || true
          docker rm bike-shop-container 2>/dev/null || true
          
          # Run new container
          docker run -d \
            --name bike-shop-container \
            -p 8080:80 \
            bike-shop-test:latest
        "
        
    - name: Verify stage1 deployment
      run: |
        ssh stage1@192.168.1.59 "
          echo '=== Docker containers ==='
          docker ps | grep bike-shop-container
          
          echo '=== Test application ==='
          curl -I http://localhost:8080 || echo 'Application not accessible yet'
        "

  deploy-production:
    runs-on: self-hosted
    needs: deploy-stage1
    if: success() && github.event.inputs.environment == 'production'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: docker-image
        
    - name: Transfer image to production server
      run: |
        IMAGE_FILE=$(ls -t *.tar | head -1)
        scp $IMAGE_FILE app@192.168.1.47:~/aplikacja_biker/
        scp -r ./bike-shop/nginx.conf app@192.168.1.47:~/aplikacja_biker/
        
    - name: Deploy to production server
      run: |
        ssh app@192.168.1.47 "
          # Load Docker image
          docker load -i ~/aplikacja_biker/$(basename $(ls -t ~/aplikacja_biker/*.tar | head -1))
          
          # Stop and remove old container
          docker stop bike-shop-container 2>/dev/null || true
          docker rm bike-shop-container 2>/dev/null || true
          
          # Run new container
          docker run -d \
            --name bike-shop-container \
            -p 8080:80 \
            bike-shop-test:latest
            
          # Configure nginx as reverse proxy
          sudo cp ~/aplikacja_biker/nginx.conf /etc/nginx/sites-available/bike-shop
          sudo ln -sf /etc/nginx/sites-available/bike-shop /etc/nginx/sites-enabled/
          sudo rm -f /etc/nginx/sites-enabled/default
          sudo nginx -t && sudo systemctl reload nginx
        "
        
    - name: Verify deployment
      run: |
        ssh app@192.168.1.47 "
          echo '=== Docker containers ==='
          docker ps | grep bike-shop-container
          
          echo '=== Nginx status ==='
          sudo systemctl status nginx --no-pager
          
          echo '=== Test application ==='
          curl -I http://localhost:80
          curl -I http://localhost:8080
        "